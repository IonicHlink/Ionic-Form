import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'form_validation_using_services',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
